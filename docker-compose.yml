# Project Equinox — local run with Docker Compose.
# Run from the repo root (where .env lives): docker compose up
# Compose injects .env into the container so the app sees your settings.

services:
  equinox:
    build: .
    ports:
      - "8081:8080"
    env_file:
      - .env
